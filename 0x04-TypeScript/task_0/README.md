# 0. Creating an interface for a student

This project implements a TypeScript interface `Student` and uses it to create an array of students, which is then rendered as an HTML table using Vanilla JavaScript.
